# state/inbox/

Items whose context is `unknown` or whose handling is not yet determined. Triage moves them to the right place under `state/`.

Every file here has `context: unknown` in frontmatter until triaged.

The Chief Operating Officer (COO) does not act on inbox items beyond proposing a triage destination and surfacing the count in briefings.
